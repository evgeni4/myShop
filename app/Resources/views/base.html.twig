<!DOCTYPE html>
<html class="ie8 oldie" lang="en">
<head>
    <meta charset="utf-8">
    <title>{% block title %}Diana’s jewelry{% endblock %}</title>
    <meta name="viewport"
          content="width=device-width, initial-scale=1, maximum-scale=1, minimum-scale=1, user-scalable=no">
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}"/>
    {# {% block stylesheets %} #}
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
    <link rel="stylesheet" href="{{ asset('css/bootstrap.css') }}">
    <link rel="stylesheet" href="{{ asset('css/font-awesome.min.css') }}">

    {# {% endblock %} #}
    <!--[if lt IE 9]>
    <script src="http://html5shiv.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
    <script src="http://code.jquery.com/jquery-1.11.1.min.js"></script>
{#    <script src="{{ asset('js/plugins.js') }}"></script>#}
    <script src="{{ asset('js/main.js') }}"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.21.0/moment.min.js" type="text/javascript"></script>

    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script src="{{ asset('js/datepicker/bootstrap-datetimepicker.js') }}"></script>
    <link rel="stylesheet" href="{{ asset('js/datepicker/bootstrap-datetimepicker.css') }}">
    <script>
        setTimeout(function () {
            $('#alertMsg').fadeOut();
        }, 2000);
    </script>
</head>
<body>
{% block header %}
    <header id="header">
        <div class="container">
            <a href="{{ path('shop_index') }}" id="logo" title="Diana’s jewelry">Diana’s jewelry</a>

            <article class="right-links">
                <ul>
                    <li>
                        <form action="">
                            <input type="text" class="form-search" name="search">
                            <button class="btn-sm btn-grey">search</button>
                        </form>
                    </li>
                    <li><a href="#"><span class="ico-products"></span>3 products, $4 500.00</a></li>
                    {% if app.user %}
                        <li><a href="{{ path('user_office') }}">
                            <span class="imgProfile">
                                {% if user.image =="" %}
                                    <img src="{{ asset('uploads/user_image/no_image.png') }}" alt="">
                                {% else %}
                                    <img src="{{ asset('uploads/user_image/'~ user.image) }}" alt="">
                                {% endif %}
                            </span>My account</a>
                        </li>
                        <li><a href="{{ path('security_logout') }}"><span class="ico-signout"></span>Logout</a></li>
                    {% else %}
                        <li><a href="{{ path('security_login') }}"><span class="ico-account"></span>Login</a></li>
                        <li><a href="{{ path('user_register') }}"><span class="ico-signout"></span>registration</a></li>
                    {% endif %}
                </ul>
        </div>
        </div>
        <!-- / container -->
    </header>
    <!-- / header -->

    <nav id="menu">
        <div class="container">
            <div class="trigger"></div>
{#            {{ render(controler('AppBundle:')) }}#}
            <ul>
                <li><a href="{{ path('new_collection') }}">New collection</a></li>
                {% for category in categories.allCategory if categories.allCategory|length>0 %}
                    <li><a href="{{ category.url }}">{{ category.title }}</a></li>
                {% endfor %}
                <li><a href="#">Promotions</a></li>
            </ul>
        </div>
        <!-- / container -->
    </nav>
    <!-- / navigation -->
{% endblock %}
{% block body %}
    {% block main %}{% endblock %}
{% endblock %}
{% block footer %}
    <footer id="footer">
        <div class="container" style="text-align: center;">
            <p class="copy">Copyright {{ 'now' | date('Y') }} Jewelry. All rights reserved.</p>
        </div>
        <!-- / container -->
    </footer>
    <!-- / footer -->
{% endblock %}
{% block javascripts %}



{% endblock %}
</body>
</html>
